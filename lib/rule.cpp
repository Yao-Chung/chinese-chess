#include <rule.hpp>

Rule::RuleReturnType BigBoardRule::validate(Chess chess, unsigned nextRow, unsigned nextCol) {
    return true;
}

Rule::RuleReturnType SmallBoardRule::validate(Chess chess, unsigned nextRow, unsigned nextCol) {
    return true;
}